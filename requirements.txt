# Core dependencies (must match pyproject.toml [project.dependencies])
anthropic>=0.40.0
openai>=1.30.0
httpx>=0.27.0
rich>=13.0.0
pyte>=0.8.0            # terminal emulator for !claude / !python bridge rendering
PyYAML>=6.0           # agent-template + plugin YAML frontmatter

# Optional — install to enable typing-time slash-command autosuggest.
# Also available as an extra: `pip install promethean[autosuggest]`.
prompt_toolkit>=3.0.43

# ── Optional features — install the matching extra (see pyproject.toml) ────
#   pip install promethean[graph]     # symbol-graph nav: diskcache, grep-ast,
#                                       pygments, tree-sitter(+language-pack),
#                                       networkx, numpy, scipy
#   pip install promethean[web]       # web UI: sqlalchemy, bcrypt, PyJWT
#   pip install promethean[files]     # docs: pymupdf, openpyxl, pypdf,
#                                       pdfminer.six, python-docx
#   pip install promethean[ocr]       # OCR: pytesseract, Pillow
#   pip install promethean[vision]    # image input: Pillow
#   pip install promethean[browser]   # web browsing: playwright
#   pip install promethean[trading]   # markets: yfinance, rank-bm25, akshare
#   pip install promethean[research]  # scholarly
#   pip install promethean[bridges]   # wechat QR: qrcode
#   pip install promethean[voice]     # STT/TTS: sounddevice, faster-whisper,
#                                       openai-whisper, ctranslate2, edge-tts, torch
#   pip install promethean[video]     # edge-tts, imageio-ffmpeg, faster-whisper,
#                                       torch, pypdf, pdfminer.six, python-docx
#   pip install promethean[dev]       # pytest
#   pip install promethean[all]       # everything above
#
# NOTE: voice/video pull torch — pip installs the CUDA build by default; on
# ROCm/CPU install the matching torch wheel first.
# Whisper model size override: export NANO_CLAUDE_WHISPER_MODEL=small
