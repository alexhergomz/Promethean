"""Promethean Web Terminal — xterm.js + WebSocket + basic auth.

Usage:
    promethean --web                  # start on port 8080
    promethean --web --port 9090      # custom port
    promethean --web --no-auth        # disable password (local only)
"""
